# nonegrame.github.io

Hexo source for <https://nonegrame.github.io/>.

## 開發

```bash
npm install
npm run serve
```

## 部署

推送到 `master` 會由 GitHub Actions 自動執行 `hexo generate` 並將產出推送到 `gh-pages` 分支，請在 GitHub Pages 設定中選擇 `gh-pages` 分支作為發佈來源。
